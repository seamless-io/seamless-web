files:
  /tmp/deployment/custom_mod_nginx.sh:
    mode: "000755"
    content: |
        sudo sed -i 's/gzip                  off;/gzip                  on;/' /etc/nginx/nginx.conf
container_commands:
  nginx_real_ip:
    command: "/tmp/deployment/custom_mod_nginx.sh"