files:
  /tmp/deployment/custom_mod_nginx.sh:
    mode: "000755"
    content: |
        sed -i 's/gzip off;/ gzip on;\n/' /etc/nginx/nginx.conf
container_commands:
  nginx_real_ip:
    command: "/tmp/deployment/custom_mod_nginx.sh"